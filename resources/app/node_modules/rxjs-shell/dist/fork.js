"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fork = void 0;
const child_process_1 = require("child_process");
const rxjs_1 = require("rxjs");
const util_1 = require("./util");
function fork(modulePath, args, options) {
    return new rxjs_1.Observable((subscriber) => {
        const proc = (0, child_process_1.fork)(modulePath, args ? args.map(String) : args, options);
        const channelSubscriptions = [];
        if (!!options && options.send instanceof rxjs_1.Subject) {
            channelSubscriptions.push(options.send.subscribe(msg => proc.send(msg)));
        }
        proc.on('message', msg => subscriber.next(msg));
        proc.on('error', err => {
            process.exitCode = 1;
            subscriber.error(new util_1.ShellError('process exited with an error', err));
        });
        proc.on('close', (code, signal) => {
            channelSubscriptions.forEach(s => s.unsubscribe());
            if (code !== 0) {
                process.exitCode = typeof code === 'number' ? code : undefined;
                subscriber.error(new util_1.ShellError(`process exited with code: ${code}`, {
                    code,
                    signal,
                }));
                return;
            }
            subscriber.complete();
        });
        const removeEvents = (0, util_1.listenTerminating)(() => subscriber.complete());
        return () => {
            channelSubscriptions.forEach(s => s.unsubscribe());
            (0, util_1.killProc)(proc);
            removeEvents();
        };
    });
}
exports.fork = fork;
//# sourceMappingURL=fork.js.map